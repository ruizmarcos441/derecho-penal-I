# Derecho Penal I

Sistema Flask para consulta de notas, prevenciones y apelaciones.

Render: `pip install -r requirements.txt` / `gunicorn app:app`

Configura `SECRET_KEY` y `TEACHER_PASSWORD` como variables de entorno.
